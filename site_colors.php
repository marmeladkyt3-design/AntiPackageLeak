<?php
/* ============================================================
   ЦВЕТА САЙТА — единый файл для всех страниц
   Меняй значения ниже — они применятся везде:
   шапка, футер, кнопки, подсветки, оверлеи и т.д.
   ============================================================ */

if (defined('SITE_COLORS_LOADED')) return;
define('SITE_COLORS_LOADED', true);

$C = array(

    /* ============ ПОДСВЕТКА ============
       Один цвет для всего, что подсвечивается:
       оверлеи, акцентные элементы, ховеры, активные пункты меню,
       переливание названия сайта и ссылок в футере. */
    'accent'        => '#6366f1',          // основной цвет подсветки
    'accent_rgb'    => '99, 102, 241',     // тот же цвет в формате R, G, B
    'accent_light'  => '#818cf8',          // светлее (ховеры, границы)
    'accent_dark'   => '#4f46e5',          // темнее (нажатия, тени)

    /* ============ ГРАДИЕНТ ============
       Градиентная заливка кнопок и выделенных блоков */
    'grad1'         => '#6366f1',          // начало градиента
    'grad2'         => '#8b5cf6',          // середина градиента
    'grad3'         => '#a78bfa',          // конец градиента
    'pink'          => '#e879f9',          // дополнительный акцент (вспышки)

    /* ============ ОСНОВНОЙ ФОН ============ */
    'bg'            => '#08080f',          // фон страницы
    'bg2'           => '#0c0c17',          // вторичный фон (секции)

    /* ============ КАРТОЧКИ И ПАНЕЛИ ============ */
    'panel'         => 'rgba(255, 255, 255, 0.028)',  // карточки и панели
    'panel_h'       => 'rgba(255, 255, 255, 0.045)',  // карточки при наведении

    /* ============ ЛИНИИ И ГРАНИЦЫ ============ */
    'line'          => 'rgba(255, 255, 255, 0.07)',   // обычные границы
    'line2'         => 'rgba(255, 255, 255, 0.13)',   // заметные границы

    /* ============ ТЕКСТ ============ */
    'ink'           => '#f1f2fa',          // заголовки, основной текст
    'ink2'          => '#b4bacd',          // обычный текст
    'dim'           => '#7d86a3',          // приглушённый текст
    'faint'         => '#4c5470',          // совсем бледный текст (подписи)

    /* ============ СТАТУСЫ ============ */
    'success'       => '#6ee7b7',          // успех (зелёный)
    'danger'        => '#f87171',          // ошибка (красный)

);